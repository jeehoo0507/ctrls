[project]
name = "ctrls"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "cjh", email = "heumheum511@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "rich>=15.0.0",
    "tomlkit>=0.15.1",
    "typer>=0.27.1",
]

[project.scripts]
cvz = "ctrls.cli:app"

[build-system]
requires = ["uv_build>=0.11.29,<0.12.0"]
build-backend = "uv_build"
